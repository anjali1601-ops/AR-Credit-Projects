# Runtime dependencies for the dispute resolution agent.
# Everything here works fully offline: no API keys, no external services.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
langchain-core>=0.3
chromadb>=0.5
sqlparse>=0.5
pypdf>=4.2
fpdf2>=2.7
jinja2>=3.1
python-dotenv>=1.0

# Test dependencies
pytest>=8
httpx>=0.27

# Optional extras, install only if you switch away from the offline defaults:
#   psycopg[binary]>=3.1   -> real PostgreSQL instead of SQLite
#   openai>=1.30           -> DRA_LLM_PROVIDER=openai
#   anthropic>=0.30        -> DRA_LLM_PROVIDER=anthropic
